# Domain Docs

Before exploring, read root `CONTEXT.md` when present and any relevant ADRs under `docs/adr/`. Missing domain files are not an error; proceed silently.

This repository uses a single-context layout:

```text
/
|-- CONTEXT.md
|-- docs/adr/
`-- src/
```

Use terms defined in `CONTEXT.md` consistently. Explicitly flag work that conflicts with an existing ADR.
