# PSD 裁片印花批量套图开发计划

版本：v1.0  
日期：2026-09-21  
状态：开发方案；尚未收到真实裁片 PSD、印花样本与工厂交付规范，尚未实施或完成 Photoshop 实测。

## 1. 先读这一页

### 要做的工具

用户先在 Photoshop 中做好一份正确的成品裁片 PSD：裁片轮廓、印花位置、缩放、方向和出血全部调好，把可替换的印花做成智能对象。工具读取用户已有的印花素材，按组替换到相应智能对象，批量导出预览和工厂要求的裁片文件。

**核心流程：做好模板一次 → 按组放入已有印花 → 自动替换 → 检查并导出。**

首版建议做成 Windows Photoshop 内的 UXP 插件面板，使用 Photoshop 原生功能渲染和保存。先完成小型验证，再开发完整面板。

### 已确定的需求

- 印花素材由用户提供，不开发 AI 生图。
- 成品 PSD 由用户制作，保存已认可的摆放关系。
- 通过智能对象实现替换，裁片轮廓和生产尺寸保持固定。
- 支持按组输入素材，并批量生成结果。
- 最终目标是输出工厂接受的印花裁片文件。

### 首版成功的标准

1. 一份真实裁片母版，三组不同印花，自动生成正确结果。
2. 位置、方向、尺寸、蒙版和出血符合已确认的模板规则。
3. 不改写母版，不混入上一组素材，不遗漏裁片。
4. 完成一轮 20 组连续处理测试，错误可定位、失败组可重试。
5. 至少一套输出通过工厂文件检查并完成实物打样；这一步由工厂配合验证。

前三项证明基本功能，第四项证明批量稳定性，第五项才证明该模板与该工艺的生产适用性。通过一个模板，不代表所有款式和工厂均已验证。

## 2. 范围与边界

| 范围                             | 首版决定                                       |
| -------------------------------- | ---------------------------------------------- |
| 一张图用于多个裁片               | 支持，使用明确的共享素材入口                   |
| 一组多张图分别用于不同部位       | 支持，例如前片、后片、袖片分别对应素材         |
| 多组素材连续处理                 | 支持，以文件夹分组                             |
| 成品预览                         | 支持裁片合版预览；已有商品效果图模板可后续接入 |
| 裁片单独导出、固定合版导出       | 按工厂要求配置，基于固定区域和可见性规则       |
| 尺码                             | 首次只验证一个尺码；其他尺码按各自认可模板处理 |
| 自动推放尺码、从照片还原纸样     | 不包含                                         |
| AI 生成印花、每组重新识别裁片    | 不包含                                         |
| 自动审美排版、主体识别与智能避让 | 后续可选，不影响首版运行                       |
| 自动对花、自动省布排料           | 不包含；模板已有对位关系可以保留，但仍需打样   |
| 云端处理、订单系统、电商发布     | 不包含                                         |

首版无需模型 API，也不上传 PSD 或印花。目录访问通过插件正常的文件选择流程建立。

## 3. 开发前需要的材料

| 材料                | 最小要求                                         | 用途                     |
| ------------------- | ------------------------------------------------ | ------------------------ |
| 真实成品母版        | 一个款式、一个尺码；图层和智能对象完整           | 验证真实操作链           |
| 已认可的输出        | 一套人工制作的合格裁片文件                       | 作为对照样本             |
| 三组印花            | 包含正常尺寸、不同长宽比、明显不同的花色         | 验证替换、适配与组间隔离 |
| 工厂交付要求        | 格式、尺寸、分辨率、色彩、出血、标记、分片或合版 | 定义输出配置             |
| 本机 Photoshop 信息 | 版本、可用磁盘、典型 PSD 大小                    | 确定兼容范围和实测环境   |

文档编写可以先完成；真实替换验证从取得母版和样本开始。工厂规范不完整时可产出测试文件，状态必须标为“待确认”，不能标为可生产。

## 4. 用户实际操作流程

### 第一次登记模板

1. 选择已保存的 PSD 母版。
2. 查看工具列出的智能对象，给可替换入口指定用途。
3. 设置哪些裁片共用同一张素材、哪些使用独立素材。
4. 确认素材适配方式、固定导出区域及工厂输出要求。
5. 选择一组印花试套，查看预览和导出结果。
6. 保存模板配置与版本。以后调用该版本即可。

不要求用户重新排版已经做好的成品 PSD。若模板结构不符合首版要求，工具指出需要调整的具体图层，而非自动重构整份文件。

### 日常批量使用

1. 选择模板。
2. 选择存放各组素材的总文件夹。
3. 检查组数、素材对应关系及缺失提示。
4. 先试套一组；新模板或新规则必须完成此步骤。
5. 选择输出目录，开始批量。
6. 查看完成、失败、待确认列表，打开结果或仅重试失败组。

同一已验证模板使用相同规则时，不要求逐组确认。运行期间显示正在处理的组和阶段，支持在安全边界暂停、停止；取消后本组未完成结果不能计入成功。

用户通过面板选择和登记，首版不要求手工编辑配置 JSON。批次报告同时在面板中用中文展示。

## 5. PSD 模板制作规范

### 5.1 三种对象必须分清

| 名称     | 含义                          | 示例                         |
| -------- | ----------------------------- | ---------------------------- |
| 裁片     | 输出的实际部位                | 前片、后片、左袖、右袖       |
| 素材入口 | 一个可替换的智能对象内容源    | 全身花纹、前片专用图         |
| 实例     | 同一内容源在 PSD 中的一次摆放 | 同一花纹分别放在前片和袖片上 |

一张素材可以供多个裁片使用；一个裁片也可能叠加底纹和其他固定元素。不能假定“一个裁片必然对应一个独立智能对象”。

### 5.2 推荐图层结构

以下名称只是新模板的建议。现有模板可在登记时映射，重名或定位不唯一时必须提示。

```text
成品母版.psd
├─ GUIDES｜辅助线与说明（生产导出按配置隐藏）
├─ PRINT｜生产内容
│  ├─ 前片（固定印花蒙版）
│  │  └─ ART｜印花智能对象实例
│  ├─ 后片（固定印花蒙版）
│  │  └─ ART｜印花智能对象实例
│  ├─ 左袖（固定印花蒙版）
│  │  └─ ART｜印花智能对象实例
│  └─ 右袖（固定印花蒙版）
│     └─ ART｜印花智能对象实例
└─ PREVIEW｜仅用于查看的背景或标注
```

每个可替换智能对象内部建议保留固定画布，仅开放一个明确的素材层，例如 `REPLACE_ART`。画布之外的缩放、旋转、裁片蒙版等属于模板结构，替换时保持不变。

生产层不应混入商品效果图的光影、褶皱或水印；哪些线条和标记需要印出来，以工厂要求为准。

### 5.3 共享与独立关系

- 共用同一花纹的部位可使用同一嵌入式智能对象的关联实例，分别保留自身位置与蒙版。
- 需要独立素材的部位必须使用独立内容源；仅改图层名称不会让内容源独立。
- 登记时显示共享关系并执行一次替换试验，确认受影响的裁片集合。
- 同一共享内容源若被分配两张不同素材，应判定为配置冲突，不能由最后一次替换覆盖前一次。
- 首版以嵌入式、可在 Photoshop 内编辑的栅格智能对象为标准。外部链接、PDF/AI 内容及复杂多层嵌套列为需整理或单独验证的情况。

Adobe 官方区分关联副本与独立副本，两者修改传播行为不同。模板登记应验证实际关系，不能只凭外观判断。[Adobe：复制智能对象](https://helpx.adobe.com/photoshop/desktop/create-manage-layers/smart-objects/duplicate-an-embedded-smart-object.html)

### 5.4 固定画布与素材适配

默认实现：进入工作副本中的智能对象，保持其内部画布尺寸及分辨率，更新约定素材层，再保存返回主文档。不同尺寸素材先按入口规则定位，不直接假定“替换内容”会获得相同构图。

| 模式               | 行为                                             | 首版支持                               |
| ------------------ | ------------------------------------------------ | -------------------------------------- |
| 严格尺寸           | 要求素材像素尺寸符合入口约定，直接按固定位置放置 | 支持，适合已标准化素材                 |
| 等比例铺满         | 等比例放大或缩小覆盖画布，按固定锚点裁去溢出部分 | 支持；登记时说明裁切行为               |
| 完整放入           | 保留整张图，可能留下空白                         | 非默认；有明确底色和允许空白规则才启用 |
| 按实际花纹大小平铺 | 依据厘米尺度重复图案                             | 后续能力，首版不默认将素材当无缝图     |

首版锚点使用中心或固定偏移，不依赖 AI 自动挑选图案主体。禁止默认非等比例拉伸。

输入图的分辨率元数据不能暗中决定最终摆放尺寸。适配规则应显式使用像素画布与模板记录的比例。重复花纹是否等大，要检查完整变换链；同源实例不天然代表物理花纹大小一致。

替换智能对象可保留既有变换，但这不等于任意尺寸的新素材都保持相同视觉构图。[Adobe：替换智能对象内容](https://helpx.adobe.com/photoshop/desktop/create-manage-layers/smart-objects/replace-the-contents-of-a-smart-object.html)

### 5.5 不随批次变化的内容

- 主文档画布、分辨率、模式、位深与色彩配置。
- 智能对象内部画布以及外部变换。
- 裁片路径、蒙版、出血范围及模板已有对位关系。
- 裁片名称、数量、导出区域和导出可见性规则。
- 固定文字、工艺标记以及其他不属于素材入口的图层。

模板登记记录上述结构基线。母版文件内容变化后，旧配置必须重新校验，不允许继续沿用失效映射。

## 6. 按组组织印花文件

### 模式 A：一张印花用于整组

```text
输入/
├─ 款式001/
│  └─ all.png
├─ 款式002/
│  └─ all.png
└─ 款式003/
   └─ all.jpg
```

`all` 对应已登记的一个或多个素材入口。扩展名来自首版支持的格式名单，不能通过文件排序猜测对应关系。

### 模式 B：一组多张印花

```text
输入/
├─ 款式001/
│  ├─ front.png
│  ├─ back.png
│  └─ sleeves.png
└─ 款式002/
   ├─ front.png
   ├─ back.png
   └─ sleeves.png
```

登记时保存 `front → 前片入口`、`back → 后片入口`、`sleeves → 左右袖入口` 的对应关系。可使用中文命名，但同一模板必须有固定匹配规则。

首版素材优先支持 PNG、JPEG。TIFF 素材或其他格式根据真实样本增加验证，不把能打开的所有格式自动列为兼容。

缺图、同一入口匹配多个文件、未配置的额外文件应明确提示。缺失必需素材时阻止该组；可选入口必须预先声明保留什么固定内容，不能默默保留上一组图案。

## 7. 输出规范与质量检查

### 7.1 输出目录

```text
输出/运行编号/
├─ 款式001/
│  ├─ 预览.jpg
│  ├─ 前片.<工厂格式>
│  ├─ 后片.<工厂格式>
│  ├─ 左袖.<工厂格式>
│  ├─ 右袖.<工厂格式>
│  ├─ 合版.<工厂格式>     # 按配置生成
│  ├─ 工作副本.psd 或 .psb # 可选；按实测配置保留可编辑结构
│  └─ result.json         # 本组记录与状态
└─ batch-report.json      # 批次完成、失败与待确认情况
```

新运行使用独立目录。文件名做非法字符和冲突检查；发现冲突不能静默覆盖。预览使用自己的导出设置，不能把缩小后的预览当生产文件。

### 7.2 固定区域导出

每个裁片登记固定的矩形导出区域，并记录原点、宽高、对应蒙版、需要显示的图层以及允许的透明或底色处理方式。

不能根据本次图案的不透明像素自动裁掉边缘，否则不同图案可能产生不同画布尺寸或坐标偏移。单片导出后的坐标变换也需保存；合版使用原来批准的固定布局。

原点坐标、像素尺寸和物理尺寸必须可核对。换算关系为：`毫米 = 像素 ÷ PPI × 25.4`。像素整数舍入误差应记录，生产公差由工厂确定。

### 7.3 输出配置必须有明确值

格式、压缩、像素尺寸、分辨率、色彩模式、位深、ICC 配置、透明背景或底色、辅助线和工艺标记的保留规则。

不默认把所有文件转成 sRGB、CMYK 或 300 PPI。基础验证可以先用 RGB/8 位样例，但真实母版的模式必须纳入兼容性验证；不兼容时明确阻止，不静默转换。

PNG、JPEG、PSD 等输出通过 Photoshop 原生能力完成；TIFF 等工厂指定格式在 M0 单独验证保存路径和参数，不能假定 DOM 已提供对应方法。只承诺实测通过的格式组合。[Adobe：Document API](https://developer.adobe.com/photoshop/uxp/2022/ps-reference/classes/document)

大文件需检查目标格式限制，并在 M0 验证保存。可编辑工作副本格式与工厂生产格式分开配置；超过 PSD 限制时，只有已验证且配置允许的任务才使用 PSB，否则提前阻止，不能静默改扩展名或格式。[Adobe：保存大型文档](https://helpx.adobe.com/photoshop/desktop/save-and-export/save-files/save-large-documents.html)

### 7.4 自动检查与人工检查分工

| 自动检查                               | 仍需人工或工厂确认         |
| -------------------------------------- | -------------------------- |
| 素材缺失、入口冲突、裁片数量           | 花纹摆放是否符合审美要求   |
| 像素尺寸、分辨率与配置匹配             | 实物色差与面料表现         |
| 蒙版、路径、变换等结构未发生非预期变化 | 缝制后位置与对花效果       |
| 输出可打开、格式正确、文件齐全         | 款式版型和生产公差是否合格 |
| 声明必须有覆盖的区域是否出现透明缺口   | 白色设计是否为有意留白     |

缺口检查应基于已登记的印花覆盖区域，允许区域按规则豁免。不能把白色像素一概判成错误，也不能承诺自动发现所有设计问题。

## 8. 技术实现方案

### 8.1 主要模块

| 模块     | 责任                                       |
| -------- | ------------------------------------------ |
| 插件面板 | 模板、输入、预览、运行、结果五步操作       |
| 模板登记 | 读取图层结构，绑定入口、共享关系和输出规则 |
| 输入分组 | 找到各组素材，预检缺失与冲突               |
| 素材替换 | 固定内部画布，适配新素材，更新主文档       |
| 原生导出 | 按固定区域和可见性生成预览及生产文件       |
| 批次管理 | 串行执行、状态记录、取消和重试             |
| 质量检查 | 检查结构、输出元数据和文件完整性           |

插件使用 UXP 和 TypeScript；优先使用 Photoshop DOM，未覆盖的操作通过实测确认的 `batchPlay` 命令完成。普通图片库可辅助校验元数据，不承担 PSD 生产渲染。[Adobe：batchPlay](https://developer.adobe.com/photoshop/uxp/ps_reference/media/batchplay/)

文档变更放入 `executeAsModal`，所有操作等待完成，批量任务按组串行，避免多个任务争抢活动文档。取消信号不应被当作普通错误吞掉。[Adobe：executeAsModal](https://developer.adobe.com/photoshop/uxp/2022/ps-reference/media/executeasmodal)

除捕获异常外，还要检查 `batchPlay` 返回的错误结果及关键操作的实际后置状态。打开智能对象会切换活动文档，执行器应明确追踪主文档与内容文档，不能依赖当前焦点猜测目标。

### 8.2 每组执行顺序

1. 校验本组素材、模板版本、可写目录和输出配置。
2. 从已保存母版文件创建新的临时工作副本，不复用上组已修改文档。
3. 在副本中重新解析图层，取得当前文档和图层 ID；不能把上一会话的 ID 直接复用。
4. 按内容源去重处理智能对象，放入素材并等待更新完成。
5. 验证结构基线、替换范围和印花覆盖。
6. 在本组临时目录导出预览和生产文件。
7. 重读输出，检查文件数量、可解码性、尺寸与必要元数据。
8. 所有必需文件通过后，提交为完成结果并写入成功记录。
9. 关闭本组创建的临时文档并释放资源；不关闭或保存用户原本打开的文档。

外部链接可能共享同一源文件，所以仅复制主 PSD 并不保证隔离。首版遇到外链应阻止并说明整理要求，不在批量运行时悄悄修改外部源文件。

### 8.3 模板与任务记录

模板配置至少保存：模板编号、版本、母版指纹、款号与尺码、文档规格、入口定位路径、共享内容源关系、内部画布规格、适配模式、固定导出区域和输出配置。

入口定位使用完整层级路径和登记时的结构特征，打开后解析成当次图层 ID。同名或结构变化导致歧义时阻止运行。

任务记录至少保存：运行编号、组编号、母版与素材指纹、配置版本、Photoshop/插件版本、当前状态、开始结束时间、输出清单、失败步骤和可读错误说明。指纹可使用文件内容哈希。

重试依据模板、素材和配置的组合指纹；有任何变化即视为新任务。不可仅凭输出目录存在就跳过。

### 8.4 失败与恢复

| 情况                     | 处理方式                                                             |
| ------------------------ | -------------------------------------------------------------------- |
| 某组缺图或图片损坏       | 该组失败并说明文件；其余合格组继续                                   |
| 某片导出失败             | 本组保持未完成，不提交部分结果为成功                                 |
| 用户停止                 | 尽快在可中断边界退出，关闭工具自己的临时文档                         |
| Photoshop 崩溃或任务中断 | 下次显示中断组；清理本任务可识别的临时状态后，从母版重跑该组         |
| Photoshop 被其他操作占用 | 暂停或有限重试，超限给出具体原因，不无限等待                         |
| 磁盘不足或输出目录不可写 | 停止队列，保留已提交结果和错误记录                                   |
| 母版或配置发生变化       | 要求重新校验并使用新版本，不沿用旧完成记录                           |
| 重启后目录访问失效       | 提示重新选择对应目录，再按指纹校验恢复，不把失效访问凭据当成文件丢失 |

只清理本工具本次运行明确创建的临时文件。生产成功状态只在必需文件全部完成后写入；提交标志的写入方式也需要做中断恢复测试。

## 9. 开发阶段与交付物

时间为单人专注开发的初步估算，不含等待真实素材、工厂反馈和打样。M0 结束后按实际 PSD 复杂度重新估算；优先通过阶段门槛，不用天数替代结果。

| 阶段              | 工作                                                             | 交付物与通过条件                                         | 初估           |
| ----------------- | ---------------------------------------------------------------- | -------------------------------------------------------- | -------------- |
| M0 真实文件验证   | 检查 PSD、宿主版本、智能对象关系；完成一次替换和工厂目标格式导出 | 原始母版未变、关联行为明确、与人工样本对照；记录支持范围 | 1–2 人日       |
| M1 单组闭环       | 实现固定内画布、素材适配、预览与固定区域导出                     | 三组素材分别正确输出，基础结构校验通过                   | 2–3 人日       |
| M2 模板登记与分组 | 登记入口映射；实现单图和多图分组、缺图/冲突预检                  | 用户可登记一个新模板，使用固定命名批量导入               | 2–3 人日       |
| M3 批量与恢复     | 串行队列、状态、停止、重试、日志和输出提交                       | 20 组连续测试及故障恢复通过，无串图、漏片、覆盖母版      | 2–3 人日       |
| M4 面板与交付     | 完善五步面板、错误提示、安装包、使用说明                         | 本机安装和重启后复用验证，交付明确版本与限制             | 1–2 人日       |
| M5 工厂验收       | 工厂检查文件，生产一个实物样品，修订模板或输出规则               | 留存样品反馈和认可的模板版本，完成生产验收               | 由工厂周期决定 |

初步开发工作量约 8–13 人日。特殊色彩模式、复杂嵌套、TIFF 参数或大文件问题可能改变估算。

M0 若不能可靠操作真实模板，先整理模板结构或验证必要的原生操作，不继续堆叠批量 UI。

## 10. 验收用例

以下均为待执行计划，不代表已经通过。需要同时保留机器检查记录与真实 Photoshop 的视觉核对证据。

| 编号 | 用例                         | 通过标准                                                                                                                 |
| ---- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| T01  | 一张花纹用于多个裁片         | 所有已绑定裁片更新，未绑定内容不变                                                                                       |
| T02  | 前片、后片、袖片分别给图     | 对应准确；不存在错误联动或后一次覆盖前一次                                                                               |
| T03  | 不同长宽比素材               | 严格尺寸模式正确拒绝不匹配素材；铺满模式按既定比例与锚点裁切                                                             |
| T04  | 相同像素、不同 PPI 元数据    | 按显式适配规则得到相同摆放结果，不意外改变尺寸                                                                           |
| T05  | 透明边缘和白色图案           | 正确识别未覆盖区域；白色设计不被误判为透明缺口                                                                           |
| T06  | 模板结构保护                 | 母版字节指纹不变；工作副本中非素材结构保持登记值                                                                         |
| T07  | 固定导出尺寸                 | 不同图案的同一裁片具有相同画布、原点规则和物理尺寸                                                                       |
| T08  | 文件格式与色彩               | 重读后格式、位深、色彩配置、尺寸符合输出配置；不支持组合被阻止                                                           |
| T09  | 缺图、重复命名、重名图层     | 精确指出问题，不随机选图或选层                                                                                           |
| T10  | A/B/C 连续执行再单独执行 A   | A 的像素结果在同一环境和规则下相符，无历史素材残留；不以容器文件字节完全相同为要求                                       |
| T11  | 20 组连续处理                | 每组必需文件齐全且可打开；记录耗时、临时文档数量、内存变化并排查持续增长                                                 |
| T12  | 中途失败与重试               | 已完成组不被覆盖；失败组从干净母版重跑；部分结果不显示成功                                                               |
| T13  | 停止和异常退出               | 正常取消及可捕获异常关闭工具临时文档；崩溃后依据中断记录恢复，仅清理确认归属的临时内容，归属不明的自动恢复文档保留并提示 |
| T14  | 模板或素材变更后恢复         | 检测版本/指纹变化，不错误跳过或混用旧产物                                                                                |
| T15  | 中文路径、磁盘不足、文件冲突 | 错误清楚；不覆盖已有文件、不产生假成功                                                                                   |
| T16  | PSD 工作副本重开             | 可重新编辑智能对象，关联与独立关系正确，未意外栅格化                                                                     |
| T17  | 工厂文件与实物确认           | 文件通过工厂检查；样品位置、颜色与接缝达到双方约定要求                                                                   |

测试素材应包含：坐标网格和方向标记、明显不同的红蓝绿图案、细线或文字、透明边缘、不同长宽比、至少一个实际生产尺寸的大图。坐标网格用于暴露错位和比例变化，不替代真实印花测试。

性能先测量再设目标。报告典型模板大小、裁片数量、输出格式、硬件环境、单组耗时与峰值内存，不预先承诺固定“每分钟多少组”。

## 11. 主要风险与处理

| 风险                               | 对策                                           |
| ---------------------------------- | ---------------------------------------------- |
| 不同尺寸素材直接替换导致构图改变   | 固定内部画布，入口显式配置适配规则             |
| 本应独立的入口实际共用内容源       | 登记关联关系，先做替换试验，冲突时阻止         |
| 多个实例花纹大小不同               | 校验各实例变换链；首次人工确定实际大小         |
| 外链更新波及母版或其他组           | 首版限定嵌入式入口，每组新建工作副本           |
| 导出自动裁边造成生产尺寸变化       | 固定区域导出，记录尺寸与坐标                   |
| 放大低清素材却被当成合格           | 根据最终物理尺寸检查有效分辨率；阈值取工厂要求 |
| 效果图光影混入生产印花             | 生产层和预览层登记独立可见性规则               |
| 大 PSD、临时文档或历史记录占用过高 | 实测大文件，串行执行并及时关闭工具临时文档     |
| 插件版本与 Photoshop 操作不兼容    | M0 固定实测版本，发布兼容表，不声称全版本可用  |
| 数字文件正确但实物偏差明显         | 以样品反馈修订模板，避免将软件验收等同生产验收 |

## 12. 最终交付清单

- 可安装的 Photoshop 插件，以及对应源代码和构建说明。
- 一份已登记、实测通过的 PSD 模板配置；原始母版由用户保留。
- 单图共享、多图分组两种输入示例与命名说明。
- 模板制作、日常使用、错误处理三部分简明说明。
- 明确的 Photoshop 版本、输入格式、智能对象结构和输出格式兼容表。
- 三组真实素材的对照结果、20 组批量报告及故障恢复记录。
- 工厂确认过的生产文件与样品反馈；若尚未取得，交付状态明确标为待工厂验收。

## 13. 开发启动顺序

先取得真实成品 PSD、三组已有印花和一套人工合格输出，执行 M0。首先证明“只替换内容即可正确输出”，再实现模板登记、批量队列和插件面板。

本方案的价值在于复用用户已经做好并认可的 PSD：把重复劳动自动化，并让每批结果可检查、可复现。
